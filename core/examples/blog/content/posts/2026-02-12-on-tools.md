---
title: On tools
date: 2026-02-12
tags: [meta, tools]
---

Tools that fade into the background let you focus on the work itself.
This blog is rendered with `tinypress build`. That's the entire pipeline.

```swift
let report = try await SiteBuilder().build(
    sourceRoot: source,
    outputRoot: out
)
```

> Lean defaults beat configurable mediocrity.
